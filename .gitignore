# node modules / dependencies — never commit
node_modules/

# package lockfiles — this plugin is installed via `dsh plugin add` from a
# directory, npm, or a GitHub source; a generated lockfile only churns the diff.
package-lock.json
pnpm-lock.yaml
yarn.lock

# downloaded ASR models (228MB) and partial downloads
models/
*.part
*.onnx

# temp / diagnostic files
*.log
_verify*
_repro*

# OS / editor
.DS_Store
Thumbs.db
